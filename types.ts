
import { LucideIcon } from 'lucide-react';

export interface MacroChallenge {
  id: string;
  title: string;
  description: string;
  icon: LucideIcon;
}

export interface DiscoveryPhase {
  week: string;
  title: string;
  description: string;
  deliverable: string;
}

